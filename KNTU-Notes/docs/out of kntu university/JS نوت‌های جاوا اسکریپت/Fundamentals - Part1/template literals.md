# 📌 Template Literals در JavaScript

## 🔹 تعریف
Template Literal نوعی رشته است که با علامت backtick (`` ` ``) نوشته می‌شود و امکانات بیشتری نسبت به `'` یا `"` دارد.

```js
const name = "MJ";
const greeting = `Hello, ${name}!`;
```
## 🔹 مزایا

### ✅ جای‌گذاری متغیر (Interpolation)



```js
const age = 18;
console.log(`MJ is ${age} years old.`);
```

### ✅ رشته چند خطی (Multiline)



```js
const msg = `Line one
Line two`;
```

### ✅ ترکیب با عبارات جاوااسکریپت



```js
const x = 5, y = 3;
console.log(`Sum is ${x + y}`);
```

## 🔹 مقایسه با `'` و `"`

|ویژگی|`'` یا `"`|`` ` ``|
|---|---|---|
|متغیر داخل رشته|❌|✅|
|چند خطی|❌|✅|
|خوانایی در رشته‌های طولانی|متوسط|عالی|

## 🔹 نکات مهم

- Template Literal فقط با `` ` `` کار می‌کند، نه با `'` یا `"`
    
- داخل `${}` می‌توان هر عبارت جاوااسکریپتی نوشت
    
- برای رشته‌های ساده، استفاده از `'` یا `"` قابل قبول است
    
- استفاده از `\n` در `'` یا `"` فقط یک کاراکتر کنترلی است، نه رشته چندخطی واقعی
    

## 🔹 مثال ترکیبی



```js
const name = "MJ";
const age = 18;
const summary = `👋 Hi ${name}!

You're ${age} years old.
Welcome to JavaScript!`;
console.log(summary);
```
