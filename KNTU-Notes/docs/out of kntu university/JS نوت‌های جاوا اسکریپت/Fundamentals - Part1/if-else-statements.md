# 🔀 شرط‌ها در JavaScript: if / else

## 🔹 تعریف
ساختار `if/else` برای اجرای کد بر اساس شرایط استفاده می‌شود. اگر شرط برقرار باشد، بخش `if` اجرا می‌شود؛ در غیر این صورت، بخش `else`.

---

## 🔹 ساختار پایه

```js
if (شرط) {
  // کدی که در صورت درست بودن شرط اجرا می‌شود
} else {
  // کدی که در صورت نادرست بودن شرط اجرا می‌شود
}
```
می‌شه که در واقع متغیر بولین رو جای شرط بذاریم چون خب جواب شرط هم در واقع بولینه ولی خب قرار دادن شرط به شکل مستقیم رایج تره
از طرفی برای ادامه دادن این ساختار اینگونه عمل میشه:

```js
const score = 75;

if (score >= 90) {
  console.log("🏆 Excellent!");
} else if (score >= 70) {
  console.log("👍 Good job!");
} else {
  console.log("📚 Keep practicing.");
}
```
